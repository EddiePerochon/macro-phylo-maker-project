% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graft-clades-also-broken.R,
%   R/graft-clades-broken.R, R/graft-clades.R
\name{graft_many_clades}
\alias{graft_many_clades}
\title{Batch grafting across one or many backbones}
\usage{
graft_many_clades(
  backbones,
  donors,
  placements,
  authority = NULL,
  chronos_select = c("auto", "fixed", "off"),
  chronos_model = "correlated",
  lambda = 1,
  lambda_grid = c(0.1, 1, 10),
  models_grid = c("correlated", "relaxed", "discrete"),
  rate_cats = c(1, 2, 5),
  calib_df = NULL,
  seed = 42,
  eps = 1e-10,
  ultrametric_final = c("extend", "nnls", "none"),
  resolve_polytomies = c("none", "donor", "backbone", "both"),
  resolve_random = TRUE,
  poly_eps = 1e-08,
  out_tree = "clade_grafted.tre",
  out_log = "clade_grafting_log.tsv",
  logger = NULL
)

graft_many_clades(
  backbones,
  donors,
  placements,
  authority = NULL,
  chronos_select = c("auto", "fixed", "off"),
  chronos_model = "correlated",
  lambda = 1,
  lambda_grid = c(0.1, 1, 10),
  models_grid = c("correlated", "relaxed", "discrete"),
  rate_cats = c(1, 2, 5),
  calib_df = NULL,
  seed = 42,
  eps = 1e-10,
  ultrametric_final = c("extend", "nnls", "none"),
  resolve_polytomies = c("none", "donor", "backbone", "both"),
  resolve_random = TRUE,
  poly_eps = 1e-08,
  out_tree = "clade_grafted.tre",
  out_log = "clade_grafting_log.tsv",
  logger = NULL
)

graft_many_clades(
  backbones,
  donors,
  placements,
  authority = NULL,
  chronos_select = c("auto", "fixed", "off"),
  chronos_model = "correlated",
  lambda = 1,
  lambda_grid = c(0.1, 1, 10),
  models_grid = c("correlated", "relaxed", "discrete"),
  rate_cats = c(1, 2, 5),
  calib_df = NULL,
  seed = 42,
  eps = 1e-10,
  ultrametric_final = c("extend", "nnls", "none"),
  resolve_polytomies = c("none", "donor", "backbone", "both"),
  resolve_random = TRUE,
  poly_eps = 1e-08,
  out_tree = "clade_grafted.tre",
  out_log = "clade_grafting_log.tsv",
  logger = NULL
)
}
\arguments{
\item{backbones}{A \code{phylo} or \code{multiPhylo} backbone chronogram.}

\item{donors}{Named list of donor \code{phylo} trees.}

\item{placements}{Named list of placement descriptors (one per donor name).}

\item{authority}{Resolved authority vector or object accepted by
\code{\link[=resolve_authority_binomials]{resolve_authority_binomials()}}.}

\item{chronos_select}{One of \code{"auto"}, \code{"fixed"}, \code{"off"}.}

\item{chronos_model}{Chronos model if \code{chronos_select="fixed"}.}

\item{lambda, lambda_grid, models_grid, rate_cats, calib_df}{Chronos controls.}

\item{seed}{Numeric seed used upstream.}

\item{eps}{Small epsilon for positional clamping.}

\item{ultrametric_final}{One of \code{"extend"}, \code{"nnls"}, \code{"none"}.}

\item{resolve_polytomies}{\code{"none"|"donor"|"backbone"|"both"}; threaded
into donor preparation.}

\item{resolve_random}{Logical; if TRUE, \code{multi2di()} resolves randomly.}

\item{poly_eps}{Numeric epsilon used to nudge edges after \code{multi2di()}.}

\item{out_tree}{Output Newick path.}

\item{out_log}{Output TSV log path.}

\item{logger}{Optional logger.}
}
\value{
A list with \code{tree} (phylo|multiPhylo) and \code{log} (data.frame).

A list with \code{tree} (phylo|multiPhylo) and \code{log} (data.frame).

A list with \code{tree} (phylo|multiPhylo) and \code{log} (data.frame).
}
\description{
Prepares donor templates (once per donor) and grafts them into each backbone
tree. Optionally enforces ultrametricity at the end (default: \code{"extend"}).

Prepares donor templates (once per donor) and grafts them into each backbone
tree. Optionally enforces ultrametricity at the end (default: \code{"extend"}).

Prepares donor templates (once per donor) and grafts them into each backbone
tree. Optionally enforces ultrametricity at the end (default: \code{"extend"}).
}
