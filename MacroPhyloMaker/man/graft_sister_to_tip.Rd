% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graft-primitives.R
\name{graft_sister_to_tip}
\alias{graft_sister_to_tip}
\title{Graft a new tip as sister to an existing tip}
\usage{
graft_sister_to_tip(
  tr,
  new_label,
  sister_to,
  shape1 = 1,
  shape2 = 1,
  min_frac = 0,
  max_frac = 1,
  .capture = FALSE
)
}
\arguments{
\item{tr}{A \code{phylo} tree.}

\item{new_label}{New tip label to add.}

\item{sister_to}{Existing tip label to which the new tip will be sister.}

\item{shape1, shape2}{Beta distribution shape parameters.}

\item{min_frac, max_frac}{Min/max fraction along the edge from child toward parent.}

\item{.capture}{If \code{TRUE}, returns list with \code{tree} and detailed \code{log} (positions, ages).}
}
\value{
Either a \code{phylo} tree or a list with \code{tree} and \code{log} if \code{.capture=TRUE}.
}
\description{
Attaches a new tip labeled \code{new_label} as sister to the tip \code{sister_to} by splitting
the incoming edge of that tip at a random fraction (Beta-distributed, optionally
constrained to the interval defined by \code{min_frac} and \code{max_frac}). If the target tip is
an immediate child of the root (no incoming edge), the function attaches the new tip as
a polytomy at that node.
}
\examples{
\dontrun{
tr <- ape::read.tree(text = "((A:1,B:1):1,(C:1,D:1):1);")
tr2 <- graft_sister_to_tip(tr, new_label = "X", sister_to = "A")
}
}
