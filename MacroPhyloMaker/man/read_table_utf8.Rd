% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/antwiki.R
\name{read_table_utf8}
\alias{read_table_utf8}
\title{Read a delimited text file (normalized to UTF-8)}
\usage{
read_table_utf8(
  path,
  sep = "\\t",
  header = TRUE,
  quote = "",
  dec = ".",
  fix_html_entities = TRUE,
  ...
)
}
\arguments{
\item{path}{Character path to the file.}

\item{sep}{Field separator (default "\t" for TSV).}

\item{header}{Logical; file has a header line? Default \code{TRUE}.}

\item{quote}{Quoting characters. For TSVs from spreadsheets, \code{""} is often safest.}

\item{dec}{Decimal mark. Default ".".}

\item{fix_html_entities}{Logical; if \code{TRUE}, replaces "&" entities (e.g., "&") with
plain \code{&} in character columns.}

\item{...}{Passed to \code{\link[utils:read.table]{utils::read.table()}}.}
}
\value{
A \code{data.frame} whose character columns are encoded in UTF-8.
}
\description{
Deterministic+heuristic decoding:
\itemize{
\item Honors \strong{UTF-16 BOM} (LE/BE) if present.
\item Detects \strong{UTF-16 without BOM} via the NUL-byte parity pattern.
\item Otherwise tries \strong{UTF-8 first}, and falls back to \strong{Windows-1252 (CP1252)} when
invalid input or replacement characters are encountered.
}

After decoding, the function strips BOM, normalizes non-breaking spaces, and parses
with \code{\link[utils:read.table]{utils::read.table()}}. All character columns are returned in UTF-8.
}
\details{
Read a delimited text file after normalizing to UTF-8 (robust, no guessing packages)
}
